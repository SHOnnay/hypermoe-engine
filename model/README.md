# Expert model directory

A populated HyperMoE expert store contains `experts.bin`, `experts.index`, and
`metadata.json`. Binary model artifacts are generated or imported locally and
should not be committed by default.
